info:
  name: All Translations
  type: http
  seq: 1

http:
  method: GET
  url: https://{{host}}/umbraco/delivery/api/v2/translations?locale=en
  headers:
    - name: Api-Key
      value: "{{Api-Key}}"
  params:
    - name: locale
      value: en
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
